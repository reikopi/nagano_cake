
  <h4>ログイン</h4>

<p>会員の方はこちらからログイン</p>
  <%= form_with model: @customer, url: customer_session_path do |f| %>
<div class="text-center">
  <table class="table">
  <tbody>

  <tr>
    <th>メールアドレス</th>
    <th><%= f.email_field :email, autofocus: true, autocomplete: "email" %></th>
  </tr>

  <tr>
    <th>パスワード</th>
    <th><%= f.password_field :password, autocomplete: "current-password" %></th>
  </tr>
  </tbody>
  </table>
  <% if devise_mapping.rememberable? %>
  remember_me<%= f.check_box :remember_me %></th>

  <% end %>

  <div class="actions">
  <%= f.submit "ログイン", class:"btn btn-primary" %>
  </div>
  <% end %>
  </div>
  <p>会員登録がお済みでない方</p>
  <div class="text-center">
  <%= link_to "こちら", new_customer_registration_path %>　から新規登録を行なってください。
  </div>