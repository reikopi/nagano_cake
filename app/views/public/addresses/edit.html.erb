<h2>編集画面</h2>

<%= form_with model:@address, url:public_adress_path(@address.id), method: :patch do |f| %>

<h4>郵便番号（ハイフンなし）</h4>
<%= f.text_field :postal_code %>

<h4>住所</h4>
<%= f.text_area :address %>

<h4>宛名</h4>
<%= f.text_field :name %>

<%= f.submit '変更内容を保存' %>