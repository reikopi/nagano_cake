<p id="notice"><%= notice %></p>

<h1>商品詳細</h1>

<p><%= image_tag @item.image %></p>
<p>商品名<%= @item.name %></p>
<p>商品説明<%= @item.introduction %></p>
<p>ジャンル<%= @item.genre.name %></p>
<p>税込価格（税抜価格）<%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)
円</p>
<p>販売ステータス
<% if @item.is_active %>
    販売中
    <% else %>
    販売停止中
    <% end %>
</p>

<%= link_to "編集する", edit_admin_item_path %>
